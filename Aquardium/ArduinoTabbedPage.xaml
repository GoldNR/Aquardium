<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Aquardium.ArduinoTabbedPage">
    <TabbedPage.Children>
        <ContentPage Title="Sensors">
            <ScrollView>
                <VerticalStackLayout Padding="20" Spacing="20">
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto">
                        <Label  Text="Status:"              FontSize="16"       Grid.Column="0"     VerticalOptions="Center"/>
                        <Label  Text="{Binding Status}"     FontSize="16"       Grid.Column="1"     VerticalOptions="Center"    x:Name="StatusValue"/>
                    </Grid>
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto">
                        <Label  Text="Temperature:"         FontSize="16"       Grid.Column="0"     VerticalOptions="Center"/>
                        <Label  Text="-- &#176;C"           FontSize="16"       Grid.Column="1"     VerticalOptions="Center"    x:Name="TemperatureValue"   x:DataType="x:String"/>
                    </Grid>
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto">
                        <Label  Text="Water Quality:"       FontSize="16"       Grid.Column="0"     VerticalOptions="Center"/>
                        <Label  Text="--"                   FontSize="16"       Grid.Column="1"     VerticalOptions="Center"    x:Name="TurbidityValue"     x:DataType="x:String"/>
                    </Grid>
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto">
                        <Label  Text="pH Level:"            FontSize="16"       Grid.Column="0"     VerticalOptions="Center"/>
                        <Label  Text="--"                    FontSize="16"       Grid.Column="1"     VerticalOptions="Center"    x:Name="pHValue"            x:DataType="x:String"/>
                    </Grid>
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto">
                        <Label  Text="Time Last Fed:"       FontSize="16"       Grid.Column="0"     VerticalOptions="Center"/>
                        <Label  Text="--"                   FontSize="16"       Grid.Column="1"     VerticalOptions="Center"    x:Name="TimeValue"          x:DataType="x:String"/>
                    </Grid>
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto"/>
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto"/>
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto"/>
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto"/>
                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto">
                        <Label  Text="Type of fish:"        FontSize="16"       Grid.Column="0"     VerticalOptions="Center"/>
                        <Entry  x:Name="TypeEntry"          Text="{Binding Type}"        Placeholder="--"            Grid.Column="1"     WidthRequest="100"  HorizontalOptions="End"/>
                    </Grid>
                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto">
                        <Label  Text="Quantity of fish:"    FontSize="16"       Grid.Column="0"     VerticalOptions="Center"/>
                        <Entry  x:Name="QuantityEntry"      Text="{Binding Quantity}"    Placeholder="--"            Grid.Column="1"     WidthRequest="100"  HorizontalOptions="End"     Keyboard="Numeric"/>
                    </Grid>
                    <Button Text="Save" HorizontalOptions="End" WidthRequest="70" Clicked="OnSaveClicked"/>
                </VerticalStackLayout>
            </ScrollView>
        </ContentPage>

        <ContentPage Title="Controls">
            <ScrollView>
                <Grid x:Name="controlButtonGrid"
                    Padding="10"
                    ColumnSpacing="10"
                    RowSpacing="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- SET CUSTOM NAME -->
                    <Button Grid.Row="0"
                        Grid.Column="0"
                        HeightRequest="100"
                        Text="Set Custom Name"
                        BackgroundColor="#2196F3"
                        Clicked="OnSetCustomNameClicked"/>          

                    <!-- SET FEED TIME -->
                    <Button Grid.Row="0"
                        Grid.Column="1"
                        HeightRequest="100"
                        Text="Set Feed Time"
                        BackgroundColor="#2196F3"
                        Clicked="OnSetFeederTimeClicked"/>

                    <!-- FEED NOW -->
                    <Button Grid.Row="1"
                        Grid.Column="0"
                        HeightRequest="100"
                        Text="Feed Now"
                        BackgroundColor="#2196F3"
                        Clicked="OnFeedNowClicked"/>

                    <!-- RESET -->
                    <Button Grid.Row="1"
                        Grid.Column="1"
                        HeightRequest="100"
                        Text="Reset Arduino"
                        BackgroundColor="#2196F3"
                        Clicked="OnResetArduinoClicked"/>

                    <!-- NEW PROFILE -->
                    <Button Grid.Row="2"
                        Grid.Column="0"
                        HeightRequest="100"
                        Text="Create New Profile"
                        BackgroundColor="#2196F3"
                        Clicked="OnNewProfileClicked"/>

                    <!-- Set PH -->
                    <!--<Button Grid.Row="2"
                        Grid.Column="0"
                        HeightRequest="100"
                        Text="Test"
                        BackgroundColor="#2196F3"
                        Clicked="OnSetPHClicked"/>-->
                </Grid>
            </ScrollView>
        </ContentPage>
    </TabbedPage.Children>
</TabbedPage>